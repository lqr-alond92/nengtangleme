# 下一步执行清单

这个文件只记录你现在应该怎么推进。完整方案见 `docs/launch/正式上线实施方案.md`。

## 当前阶段

阶段 1：后端基础。

目标：让「能躺了吗」从本地 localStorage 原型，变成可登录、可云端保存的 Web App。

当前实施状态：

- 已确认云厂商方向：阿里云；
- 已确认上线主体：个人；
- 已从截图确认已有 OSS Bucket：`neng-tang-le-ma-1158245633`，区域为华东 2（上海）；
- 已确认正式域名：`nengtangleme.cn`；
- 域名、ICP备案、公安备案、DNS、HTTPS 的单独清单见 `docs/launch/nengtangleme-cn-备案上线清单.md`。

## 你现在先做这 7 件事

1. 确认第一版只做 Web App / PWA，不做原生 App。
2. 云厂商已按阿里云推进。
3. 在阿里云确认 `nengtangleme.cn` 已完成域名实名认证。
4. 上线主体已按个人推进。
5. 开通阿里云百炼 / DashScope。
6. 准备一个测试用的通义千问 API Key。
7. 确认登录方式：邮箱验证码优先，手机号验证码需要额外开短信服务。

## 让 Codex 接着做的第一张任务单

```text
请开始阶段 1：后端基础。

目标：
把当前 App 从 localStorage 保存升级为“用户登录后云端保存”。

范围：
1. 新建后端服务；
2. 新建数据库 schema；
3. 增加用户登录接口；
4. 增加计划保存接口；
5. 增加目标保存接口；
6. 前端增加登录态；
7. 前端把 plan 保存到后端；
8. localStorage 只作为未登录草稿。

暂时不要接入通义千问。
暂时不要做支付。
暂时不要做原生 App。
```

## 阶段 1 完成标准

- 用户可以登录；
- 用户可以保存当前家庭计划；
- 刷新页面后数据还在；
- 换浏览器登录后能看到同一份计划；
- 未登录用户仍可用本地草稿；
- 测试和构建通过。

## 阶段 1 完成后再做什么

阶段 2：计算迁移。

目标：把 `finance.mjs` 的计算能力迁移到后端 API，前端只展示后端返回的结果。

阶段 3：通义千问接入。

目标：让结果页解释和继续问 AI 走后端模型接口。

阶段 4：合规页面。

目标：补齐用户协议、隐私政策、AI 使用说明、非投资建议免责声明、模型公示、账号注销说明。

阶段 5：内测部署。

目标：部署到内测环境，让 20-50 个真实用户使用。

## 你不用现在操心的事

- App Store 上架；
- Android 应用市场；
- 小程序；
- 付费订阅；
- 多模型切换；
- 银行账户同步；
- 复杂后台。

这些都放到内测跑通之后再做。

## 阿里云部署规划

域名、DNS、ECS、OSS、HTTPS、备案的关系见：

`docs/launch/阿里云部署架构与域名解析规划.md`

`nengtangleme.cn` 的实名、ICP备案、公安备案、备案服务码、DNS 和 HTTPS 执行清单见：

`docs/launch/nengtangleme-cn-备案上线清单.md`
